const main = document.getElementById("root")

const letters = ['a', 'b', 'c', 'd', 'e']

const userLetter = prompt ("Write the letter down")

console.log(letters)
console.log(userLetter)

console.log(letters.innerText)
// main.append(promptLetter)